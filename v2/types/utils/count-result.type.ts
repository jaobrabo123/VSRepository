/**
 * Return type of batch operations that report how many records were affected,
 * such as `removeList`, `softRemoveList`, and `restoreList`.
 *
 * @publicApi
 */
export type CountResult = { count: number };
